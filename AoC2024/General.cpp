#include <iostream>
#include <fstream>
#include <string>
#include <cmath>
#include <algorithm>
#include <numeric>
#include <vector>
#include <deque>
#include <queue>
#include <stack>
#include <set>
#include <map>
#include <list>
#include <unordered_map>
#include <unordered_set>
#include <iterator>
#include <utility>
#include <array>
#include <bitset>
#include <iomanip>
#include <limits>
#include <cstdlib>
#include <random>

using namespace std;

int main() {

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int i, j, k;                       // loop indices
    int n, m;                          // input sizes
    double tol = 1e-5;                 // tolerance for equality
    string v, w;                       // input strings
    char c, d;                         // input chars
    vector<int> vec;                   // input vector
    vector<vector<int>> vecvec;        // input 2D vector
    map<int, int> tmap;                // example map for counting or storing key-value pairs
    list<int> l;                       // int list
    set<int> tset;                     // example set for unique elements
    unordered_map<string, int> tumap;  // example unordered map for faster lookups
    stack<int> tstack;                 // example stack for LIFO operations
    queue<int> tqueue;                 // example queue for FIFO operations

    cin >> n;

    for (i = 0; i < n; i++) {
        // do something
    }


    return 0;
}